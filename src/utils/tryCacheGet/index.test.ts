import * as mocha from 'mocha';
import { expect } from 'chai';
import * as Utils from "./"

// describe('Utils', () => {
//     describe('tryCacheGet', () => {
//         it('should throw when any value is null', () => {
//             expect(() => Ensure.notNull(null)).to.throw('Value can not be null');
//         });

//         it('should do nothing when all values are not null', () => {
//             expect(() => Ensure.notNull(1)).to.not.throw();
//         });
//     });
// });